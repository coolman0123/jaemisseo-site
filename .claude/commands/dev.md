# Start Development

Start both the Metro bundler and Cloudflare Workers dev server for full-stack development.

1. Start Metro bundler: `npm start`
2. In a separate terminal, start the API: `cd cloudflare && npm run dev`
3. Run the iOS app: `npm run ios` or Android: `npm run android`
